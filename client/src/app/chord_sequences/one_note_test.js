import {
  Note,
  Chord
} from '../../lib';

const chordSequence = [
  new Chord([
    new Note("G", 4, "flat")
  ]),
  new Chord([
    new Note("C", 5, "sharp")
  ]),
  new Chord([
    new Note("A", 4)
  ]),
];

export default chordSequence;